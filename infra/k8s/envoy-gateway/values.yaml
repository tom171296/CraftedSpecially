# Envoy Gateway Helm values
# Chart: oci://docker.io/envoyproxy/gateway-helm
# Namespace: envoy-gateway-system
#
# Install:
#   helm upgrade --install envoy-gateway oci://docker.io/envoyproxy/gateway-helm \
#     --namespace envoy-gateway-system --create-namespace \
#     --version v1.3.0 \
#     --values infra/k8s/envoy-gateway/values.yaml

# Gateway controller: 2 replicas with zone spreading for HA.
deployment:
  replicas: 2
  pod:
    nodeSelector:
      agentpool: workloadpool
    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  control-plane: envoy-gateway
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            control-plane: envoy-gateway

config:
  envoyGateway:
    provider:
      type: Kubernetes
      kubernetes:
        # The managed Envoy proxy deployment settings.
        envoyDeployment:
          replicas: 2
          pod:
            # Run proxy pods on the workload node pool.
            nodeSelector:
              agentpool: workloadpool
        envoyHpa:
          minReplicas: 2
          maxReplicas: 5